# Contributing

All changes must go through a reviewed pull request. Never push to `main`.
The sandbox must remain enabled at all times. Autonomy level for this
repository is 3 (bounded task agent); every consequential action requires
human approval before it runs.

Open an issue before starting non-trivial work, and keep pull requests small
and focused.
