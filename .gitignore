# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.python-version

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets and local environment
.env

# coursectl clones target repositories here (spec 10.5); anchored to the
# repo root so Go packages like coursectl/internal/workspace stay tracked
/workspace/

# scripts/bootstrap.{sh,ps1} extract the coursectl binary here
/bin/

# Execution traces are local artifacts, except the committed examples
traces/*
!traces/examples/
